import React from 'react';
import styled from "styled-components";

const ProductDetailContainer = styled.div`
  width: 100%;
  min-height: 6em;
  display: flex;
  padding: 6px 8px;
  align-items: center;
`;

// props로 productInfo를 받아온다.
export function ProductDetail(props) {
  return (
    <div>
    </div> 
  );
}
